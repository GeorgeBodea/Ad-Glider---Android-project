<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="@dimen/small_distance"
    android:layout_marginHorizontal="@dimen/extra_small_distance"
    card_view:cardBackgroundColor="@color/cloud"
    card_view:cardCornerRadius="@dimen/small_distance"
    card_view:cardElevation="@dimen/small_distance">

    <ImageView
        android:id="@+id/google_play_icon"
        android:layout_width="330dp"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:layout_marginStart="90dp"
        android:src="@drawable/get_it_on_google_play"
        android:contentDescription= "@string/this_is_an_ad_description" />

    <LinearLayout
        android:layout_margin="@dimen/small_distance"
        android:layout_width="184dp"
        android:layout_height="130dp"
        android:orientation="vertical"
        android:padding="@dimen/small_distance">

        <TextView
            android:id="@+id/adTitleTextView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/small_distance"
            tools:text="Title" />

        <TextView
            android:id="@+id/adCompanyTextView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/small_distance"
            android:layout_marginVertical="@dimen/extra_small_distance"
            tools:text="CompanyName" />

        <TextView
            android:id="@+id/adBodyTextView"
            android:layout_width="match_parent"
            android:layout_height="@dimen/medium_to_large_distance"
            tools:text="Body" />
    </LinearLayout>


</androidx.cardview.widget.CardView>